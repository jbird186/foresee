#%{over over / * -}

#false{0}
#true{1}
#not{1 ^}
#!={== not}
#<={> not}
#>={< not}

#dup{0 pick}
#over{1 pick}
#swap{1 roll}
#rot{2 roll}

#dowhile(__ops __cond) {
    __ops
    while __cond {
        __ops
    }
}

#for(__init __cond __iter __ops) {
    __init
    while __cond {
        __ops
        __iter
    }
}